@model Paperfeed.Models.RoleViewModel

@{
    ViewBag.Title = "Create";
}

<!--TODO: Buttons - Gjøre at det kommer notification når man submitter på bunnen (bruk mdl)-->

<h2>Create Role</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div id="role">
        <h4>Role</h4>
        <hr/>
        @Html.ValidationSummary(true)

        <div class="mdl-textfield mdl-js-textfield">
            @Html.LabelFor(model => model.Name, new {@class = "mdl-textfield__label"})

            @Html.TextBoxFor(model => model.Name, new {@class = "mdl-textfield__input"})

        </div>
        <br/>
        @Html.ValidationMessageFor(model => model.Name)
        <button type="submit" value="Create" class="mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect">Submit</button>

    </div>
}


<div class="roleTilbakeKnapp">
    <form method="get" action="@Url.Action("Roles", "Admin")">

        <button class="mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect">
            Back to List
        </button>

    </form>
</div>
@*<div>
    @Html.ActionLink("Back to List", "Roles")
</div>*@